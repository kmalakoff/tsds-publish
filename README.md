# tsds-publish

Internal npm publishing command used by `ts-dev-stack`.

Install and configure `ts-dev-stack` to use this command. See the
[ts-dev-stack documentation](https://www.npmjs.com/package/ts-dev-stack).
